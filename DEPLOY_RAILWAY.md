# راهنمای دیپلوی روی Railway

## ۱) آپلود روی GitHub
1. وارد سایت **github.com** شوید و یک **ریپازیتوری جدید (New repository)** بسازید (مثلاً نام: `my-vpn-panel`). می‌توانید Private باشد.
2. محتوای همین پوشه را در ریپازیتوری خود آپلود کنید (کد را یکجا آپلود کنید — نه فقط بعضی فایل‌ها).
   - اگر با گیت آشنا هستید:
     ```bash
     git init
     git add .
     git commit -m "panel"
     git branch -M main
     git remote add origin https://github.com/UserName/my-vpn-panel.git
     git push -u origin main
     ```

## ۲) ساخت پروژه در Railway
1. وارد **railway.com** شوید و روی **New Project** بزنید.
2. گزینه **Deploy from GitHub repo** را انتخاب کنید.
3. ریپازیتوری خود را انتخاب کنید و روی **Deploy** بزنید.
4. منتظر بمانید تا بیلد تمام شود (دکمه **View Logs** را بزنید؛ باید `Spider Gateway ... started on port 8080` را ببینید).

## ۳) تنظیم دامنه و پورت
1. روی سرویس خود بزنید → تب **Settings**.
2. روی **Generate Domain** کلیک کنید تا یک دامنه مثل `xyz.up.railway.app` بسازد.
3. در **Networking** مطمئن شوید پورت **8080** است (خودکار باید باشد).
4. اگر خطای «Restarting» دیدید، یک‌بار سرویس را Restart کنید.

## ۴) ورود به پنل
- آدرس پنل: `https://دامنه-شما/spider`
- رمز پیش‌فرض: `123456` (بعد از ورود حتماً از تنظیمات عوض کنید).

## ۵) برنامه مشتری (برای فروش به مشتری)
- مشتری‌ها از این آدرس استفاده می‌کنند: `https://دامنه-شما/app`
- هر کاربر یک کد دارد (نام کاربری یا کد اشتراک همانی است که هنگام ساخت کاربر ساخته می‌شود).
- برنامه به‌صورت خودکار آدرس پنل را می‌خواند؛ اگر پنل جدید بسازید همین برنامه بدون تغییر کار می‌کند.

## نکته‌ها
- برای کارکردن کامل VPN (ورکر کلودفلر)، بعد از ورود به پنل بخش Worker را تنظیم/همگام کنید.
- اگر رمز پیش‌فرض را عوض نکنید، هر کسی با `123456` وارد می‌شود!
